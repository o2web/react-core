// const redirects = [
// { from: '/en/route-one', to: '/en/redirect-one' }
// { from: '/en/about/', to: '/en/artworks/', parent: true, external: true }
// { from: '/en/route', to: 'http://exemple.ca', external: true }
// ];
const redirects = [];

export default redirects;
